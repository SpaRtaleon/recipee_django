{
    "builds": [{
        "src": "enterprise/wsgi.py",
        "use": "@natanffs/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" ,"output": "build"}
    
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "enterprise/wsgi.py"
        }
    ]
}