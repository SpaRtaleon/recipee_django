{
    "builds": [{
        "src": "enterprise/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "enterprise/wsgi.py"
        }
    ]
}